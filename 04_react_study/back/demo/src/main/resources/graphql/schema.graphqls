type Query {
    getGraphql(id: ID!): Graphql
}

type Mutation {
    createGraphql(writer: String!, title: String!, contents: String!): Graphql
}
type Graphql {
    message: String!
}

type Response {
    writer: String
    title: String
    contents: String
}